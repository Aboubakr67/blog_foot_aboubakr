{% extends 'base.html.twig' %}

{% block title %}Mon Profil
{% endblock %}

{% block body %}
	<div class="container mt-5 mb-5">
		<h1 class="mb-4 text-center">Mon Profil</h1>

		<!-- Section de profil -->
		<div
			class="text-center mb-4">
			<!-- Image de profil -->
			<img src="{{ asset('images/default-profile.png') }}" alt="Image de profil" class="img-fluid rounded-circle profile-img">
		</div>

		<div class="row justify-content-center">
			<div
				class="col-md-8">
				<!-- Informations utilisateur -->
				<div class="card">
					<div class="card-body">
						<h5 class="card-title">Informations de l'utilisateur</h5>
						<p class="card-text">
							<strong>Email :</strong>
							{{ user.email }}<br>
							<strong>Nom d’utilisateur :</strong>
							{{ user.username }}<br>
						</p>
						<!-- Bouton de modification -->
						{# <a href="{{ path('app_profile_edit') }}" class="btn btn-primary">Modifier mon profil</a> #}
					</div>
				</div>
			</div>
		</div>
	</div>
{% endblock %}
